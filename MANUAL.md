# 이슈 루프 사용자 매뉴얼

## 이 루프가 하는 일

Claude에게 이슈를 맡기면 설계 에이전트가 작업을 나누고, 구현 에이전트가 한 항목씩 구현한다.
설계 에이전트는 구현 결과를 검토하고 보완하거나 다음 항목으로 넘어간다.
코드와 작업 문서를 저장하므로 중간에 멈춰도 이어서 진행할 수 있다.

## 준비물

macOS 또는 Linux에서 다음을 준비한다.

- Herdr와 Git, Python 3.
- Claude Code의 `claude`와 Codex의 `codex` CLI. 기본 구성은 설계에 Claude, 구현에 Codex를 쓴다.
- 구현 대상 Git 저장소와 이슈 번호.
- GitHub 저장소라면 `gh`, GitLab 저장소라면 `glab`의 이슈 조회 인증.

에이전트가 표시하는 권한 승인과 프로젝트 신뢰 확인은 사용자가 직접 처리한다.
루프는 승인 창을 자동으로 통과시키지 않는다.

## 설치

이 저장소를 내려받은 폴더에서 다음 명령을 실행한다.
`issue-loop` 링크가 이미 있다면 새로 만들기 전에 연결된 위치를 확인한다.

```sh
mkdir -p ~/.claude/skills
ln -s "$(pwd)" ~/.claude/skills/issue-loop
```

저장소 폴더를 그대로 연결하므로 해당 저장소를 업데이트하면 스킬에도 반영된다.

## 첫 실행

Herdr pane에서 구현 대상 프로젝트 폴더로 이동해 Claude Code를 연다.
Claude에게 이슈 번호와 함께 요청한다.

> 이 저장소의 123번 이슈 루프를 돌려줘.

다른 저장소의 이슈라면 전체 이슈 URL을 전달한다.
스킬이 실행 환경과 인증을 확인하고, 별도 Git worktree를 준비한 뒤 실행 명령을 보여준다.
기존 worktree가 있으면 재사용하고, 없으면 새로 만든다.

## 실행 중 생기는 것

실제 변경은 주 저장소와 분리된 worktree에서 이루어진다.
기본 worktree 폴더는 주 저장소 옆의 `<저장소 이름>-issue-<번호>`이고,
작업 문서는 그 안의 `tmp/issue-<번호>`에 저장된다.

| 파일 또는 폴더 | 담는 내용 |
|---|---|
| `design.md` | 목적, 설계와 수용 조건 |
| `tasks.md` | 구현할 작업과 상태 |
| `progress.md` | 구현 결과와 검토 기록 |
| `runs/` | 호출별 프롬프트와 결과 JSON |

프로젝트가 `tmp/`를 Git에서 무시하지 않는다면 작업 문서도 변경 목록에 나타날 수 있다.
스킬의 안내를 확인하고 나중에 커밋할 파일을 선택한다.

## 얼마나 돌릴지 정하기

기본 실행은 설계 후 한 항목을 구현하고 검토까지 마친 뒤 멈춘다.
원하는 범위와 횟수를 대화로 지정할 수 있다.

| 원하는 범위 | 요청 예시 |
|---|---|
| 설계만 | “123번 이슈를 설계까지만 진행해줘.” |
| 구현까지 | “123번 이슈를 설계하고 첫 항목 구현까지만 해줘.” |
| 검토까지 반복 | “123번 이슈를 구현과 검토까지 3회 반복해줘.” |

횟수는 완료한 작업 수와 다르다. 같은 항목을 보완하는 구현도 한 번으로 센다.
전체 작업이 끝나거나 오류 또는 승인 대기가 발생하면 요청한 횟수보다 먼저 멈출 수 있다.
지정한 범위에서 정상적으로 멈췄다면 “같은 이슈 루프를 이어서 진행해줘”라고 요청한다.

## 멈췄을 때

먼저 Claude가 전달한 종료 메시지를 확인한다.
승인 대기나 타임아웃으로 중단됐다면 다음 순서로 진행한다.

1. Claude에게 “중단 원인과 확인할 pane을 알려줘”라고 요청한다.
2. 해당 pane에서 작업이 계속되는지 직접 확인하고, 필요한 승인을 처리한다.
3. 구현 pane은 작업 상태를 확인한 뒤 종료한다. 설계 pane은 입력 대기 상태로 만든다.
4. 확인한 사실과 재개 결정을 Claude에게 전달한다.

> 구현 pane의 작업이 멈춘 것을 확인하고 닫았어. 설계 pane은 입력 대기 중이야.
> 이 상태로 123번 이슈 루프를 재개해줘.

타임아웃은 에이전트 종료를 뜻하지 않는다. pane 확인 없이 재개하지 않는다.
재개할 때는 저장된 코드와 문서를 바탕으로 진행하며, 최초 설계 중 중단됐다면 설계를 이어간다.

## 끝난 뒤

지정 횟수까지 실행했다는 메시지와 이슈 전체를 완료했다는 메시지는 구분해서 읽는다.
전체 완료 여부는 설계 에이전트가 수용 조건을 최종 검토한 결과로 판단한다.

worktree의 변경 사항과 `progress.md`의 검증 결과를 확인한다.
커밋, 푸시와 PR 또는 MR 제출은 자동으로 하지 않으므로 사용자가 결정한다.
전체 완료 시에는 설계 pane이 자동으로 닫힌다. 화면에 남은 pane이 없으면 끝난 것이다.
지정 횟수까지만 실행하고 멈춘 경우에는 설계 pane이 남아 다음 실행에서 그 대화를 이어간다.

## 다른 이슈로 바꾸기

같은 worktree에 미완료 작업이 있으면 다른 작업을 시작할 수 없다.
앞선 작업을 포기하려는 경우에만 해제를 요청한다.

기존 구현 pane을 종료하고 설계 pane을 입력 대기 또는 완료 상태로 만든 뒤 말한다.

> 현재 이슈 작업을 포기할게. 이 worktree의 루프를 해제해줘.

해제해도 작업 문서와 호출 기록은 남는다.
같은 작업 폴더로 다시 실행하면 최초 설계부터 시작한다.

## 자주 막히는 곳

| 증상 | 대처 |
|---|---|
| Herdr 밖이라 실행이 거부됨 | Herdr pane 안에서 Claude Code를 열고 다시 요청한다. |
| CLI를 찾지 못함 | 사용할 `claude`, `codex`와 `herdr`가 설치되어 실행 가능한지 확인한다. |
| 이슈 조회 인증 실패 | 해당 저장소의 `gh auth status` 또는 `glab auth status`를 확인하고 로그인한다. |
| 승인 대기 또는 타임아웃 | 해당 pane을 직접 확인한 뒤, 확인한 상태와 재개 결정을 전달한다. |
| 지정 횟수만 실행하고 멈춤 | 정상 중단이라면 같은 이슈를 이어서 진행해 달라고 요청한다. |
| 미완료 작업 때문에 새 작업이 거부됨 | 기존 작업을 이어가거나, 포기할 경우 pane 상태를 확인하고 해제를 요청한다. |

직접 실행하는 방법과 세부 옵션, 상태 파일 위치는 [README](README.md)를 참고한다.
